// Este endpoint ya no es necesario porque el login ahora se realiza en el frontend directamente con Supabase.
// Puedes eliminar este archivo de forma segura.

export const dynamic = "force-static"; // Para evitar advertencias de Next.js
